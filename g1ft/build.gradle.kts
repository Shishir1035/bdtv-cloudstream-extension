// Use an integer for version numbers
version = 1

cloudstream {
    description = "Live stream aggregator (M3U) — set F."
    authors = listOf("Shishir1035")

    /**
     * Status int as one of the following:
     * 0: Down, 1: Ok, 2: Slow, 3: Beta-only
     */
    status = 1

    tvTypes = listOf("Live")
    iconUrl = "https://www.google.com/s2/favicons?domain=toffeelive.com&sz=%size%"
    isCrossPlatform = true
}
